# macOS helper scripts (examples)

These files are examples and should be copied to your local machine.

Recommended:
- copy to `~/bin/`
- `chmod +x`
- add `~/bin` to PATH in `~/.zshrc`

Security:
- do NOT commit private keys
- scripts reference local key paths only
