# Example sudoers drop-in (server side)
# Place as: /etc/sudoers.d/10-grid-timeout
# Use visudo -c to validate.
#
# 120 minutes sudo timestamp timeout for grid + grid-ops.

Defaults    timestamp_timeout=120
Defaults:grid     timestamp_timeout=120
Defaults:grid-ops  timestamp_timeout=120
