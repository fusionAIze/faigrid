{
    email ${ACME_EMAIL}
}

# n8n External Node
${N8N_HOST} {
    reverse_proxy grid-external-n8n:5678
}

# Plane.so External Node
${PLANE_HOST} {
    # Web Frontend
    reverse_proxy grid-plane-web:3000

    # API handles /api
    handle /api/* {
        reverse_proxy grid-plane-api:8000
    }

    # SECURE DASHBOARD (Basic Auth)
    handle /stats/* {
        basicauth {
            # User: grid, Password: generate via 'caddy hash-password'
            grid ${CADDY_STATS_PWD_HASH}
        }
        root * /var/www/faigrid
        file_server browse
    }
}
